{
  "name": "task-manager-frontend",
  "version": "1.0.0",
  "private": true,
  "description": "Vanilla JS frontend for Task Manager",
  "scripts": {
    "dev": "live-server --port=5500 --open=login.html"
  },
  "devDependencies": {
    "live-server": "^1.2.2"
  }
}
